namespace LightInvestTest.Description;


/**
 * Задание №2 - описание и схема работы.
 */
public class Task2_OrderNotify
{
    
    /*
     *
                    [ Клиент / Postman ]
                             │
                             │ 1. POST /api/orders (Отправляет JSON: Symbol, Price, Volume, UserId)
                             ▼
                     ┌──────────────────────────────────────────────┐
                     *             OrdersController                 *
                     *  - Проверяет валидность и «доступ»           *
                     *  - Вызывает OrderService.CreateOrderAsync()  *
                     └──────────────────────┬───────────────────────┘
                                            │
                                            ▼
                     ┌──────────────────────────────────────────────┐
                     *              OrderService                    *
                     *  - Создает объект Order (IsActive = true)    *
                     *  - Сохраняет в память (ConcurrentDictionary)  *
                     └──────┬───────────────────────────────┬───────┘
                            │                               │
                            │ 2. Возвращает созданный ордер │ 3. Через HubContext шлет уведомление
                            ▼                               ▼
                     [ Ответ клиенту (HTTP 200 OK) ]   [ OrdersHub (WebSocket / SignalR) ]
                                                            │
                                                            ▼
                                               ( Рассылка всем клиентам )
                                               ( пользователя в его группу )

                            ══════════════ ФОНОВЫЙ ПРОЦЕСС ══════════════

                     ┌──────────────────────────────────────────────┐
                     *      OrderExpirationBackgroundService        *
                     *  - Каждую секунду проверяет ордера в памяти  *
                     *  - Ищет активные ордера старше 30 секунд     *
                     └──────────────────────┬───────────────────────┘
                                            │
                                            ▼ (Нашел ордер, прошло > 30 сек)
                     ┌──────────────────────────────────────────────┐
                     *            OrderService.CancelAsync          *
                     *  - Меняет IsActive = false                   *
                     └──────────────────────┬───────────────────────┘
                                            │
                                            ▼
                     ┌──────────────────────────────────────────────┐
                     *             OrdersHub (SignalR)              *
                     *  - Отправляет клиенту обновление ордера      *
                     *    (теперь IsActive: false)                  *
                     └──────────────────────────────────────────────┘
     * 
     */
    
}